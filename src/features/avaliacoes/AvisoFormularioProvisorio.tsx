export function AvisoFormularioProvisorio() {
  return <aside role="status" className="rounded-lg border-2 border-current p-4"><h2 className="font-semibold">Formulário em desenvolvimento</h2><p className="mt-1 text-sm">Esta tela ainda não salva avaliações. O backend não publicou o contrato necessário para o novo modelo de avaliação. Use esta página apenas para validar o preenchimento — nenhum dado é enviado ao servidor.</p></aside>;
}
